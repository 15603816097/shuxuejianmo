# 02 候选模型比较

## 1. 选择依据

以下比较依据01的实际数据审查，不按题型机械套模型。共有15个服务区、80箱、3种运输机型、8架运输机、14组分型电池、2架中继、6组中继能源；原始DEM近195万像元。真正的组合规模来自“箱批—访问顺序—设备—能源—时间—通信”的耦合，不是仅由15个点决定。

共用地形、能耗、充电与通信公式是题给规则的实现及必要闭合解释，不是额外堆叠的“创新模型”。不采用机器学习：80箱是设计任务，不是80个飞行能耗/通信结果训练样本；3行机型参数也不支持统计学习。没有历史时序，不做需求预测或LSTM。优先系数来自附件，不用AHP/熵权法重新生成需求权重；资源采购单价未给，不伪造经济成本。

多目标优先采用“硬约束先满足，主目标优化，ε约束展示交换代价”。不同量纲指标不直接无说明相加。下面的时间成本是相对实施工作量，不是已测运行时间，也不保证具体机器上能在某秒内求完。

## 2. 问题一：安全能力与单区组批

共同前置：对45个“机型—服务区”组合，以DEM确定的去/回航段计算单调能耗函数，二分安全载荷边界。该步骤无需回归或启发式搜索。

- **A：可行批次枚举 + 集合划分整数规划（主选）**。每区独立列出箱子子集与机型；先滤质量、体积、能量，再用每箱恰一次约束选批。S001最多15箱，最多32767个非空子集/机型，其他区最多8箱；同属性计数可进一步减少重复枚举。
- **B：按物资类别数量状态的多目标动态规划（备用）**。Q1不含逐箱时限，S001四类需求(2,8,3,2)，只有(2+1)(8+1)(3+1)(2+1)=324个计数状态；可在状态上递推可行装载模式，再恢复箱号。必须保留完整机型能耗代价，不能只追求装满重量。
- **C：双容量最佳适配递减 + 换型/拆并批局部搜索（基准）**。优先把大体积/重箱装入可行机型，局部交换改善。用于快速对照，不能代替小规模精确模型。

|比较维度|A：集合划分|B：数量状态DP|C：贪心+局部搜索|
|---|---|---|---|
|理论适用性|直接表达每箱一次、异构机型和可行批次|同区同类别箱属性一致，状态具有可加代价|适用装箱构造，最优性弱|
|实际数据匹配|最多15箱，完整枚举可控|四类别且重复箱多，尤其适合S001|80箱不大，但精确求解优势更明显|
|满足题目约束|批次预检+覆盖等式明确保证|可行模式过滤后递推保证|须每次交换后重算体积与能耗|
|可解释性|每列就是一个真实架次|状态为剩余各类箱数，易展示|规则直观，但搜索停点解释弱|
|计算复杂度|候选至多3Σ(2^ni−1)，整数求解最坏指数|O(状态数×可行模式数)，多目标标签可能增多|构造近似O(n²)，局部搜索依邻域而定|
|实现风险|列过多、重复批次、漏掉慢型非支配模式|回溯箱号、Pareto标签爆炸|早期贪心造成额外架次|
|参数敏感|对安全余量和目标ε敏感，无随机种子|对余量和目标优先顺序敏感|对箱排序、搜索次序敏感|
|验证难度|小区穷举和LP下界可交叉核对|可与A逐区交叉验证|只能验证可行，不易判断距最优多少|
|比赛时间成本|低至中；物理层复用后直接建立|低至中；本题状态小|低；适合基线|
|论文表达|能力边界、可行域与多目标权衡完整|状态递推清楚，但要解释等价压缩|单用容易缺少优化证据|

选择A是为了逐箱可追溯和与后续路线列统一。若整数求解工具不便，B足以完整替代，并非低质量退路。Q1不能把“某型最大载荷”当唯一装箱容量，体积往往更先约束A型。

## 3. 问题二：多点运输与共享电池调度

- **A：可行路线列集合划分 + 连续时间资源调度MILP + 大邻域扩列（主选）**。路线列固定箱集合、访问序列及机型，预先用真实剩余载荷算能耗、各箱相对交付时刻与充电时间。主问题选列并排实体/电池；从单点列和短多点列出发，用移箱、拆并批、换型、访问次序修改扩充候选。
- **B：逐箱/逐弧的一体化混合整数非线性规划（MINLP，精确参照）**。直接把机型、剩余载荷、路径与开始时刻放入模型，保留q^(3/2)和电池析取。结构完整，但80箱、重复访问与多趟资源使模型难调，适合作为数学原型或缩小实例基准。
- **C：截止优先构造 + 自适应大邻域搜索ALNS + 资源事件解码器（备用）**。以31个硬截止箱先构造，其他箱后插入；每次邻域改变后，按实体和电池可用时刻排程并核查所有后续任务。多随机种子评估稳定性。

|比较维度|A：路线列+MILP修复|B：一体化MINLP|C：ALNS+解码|
|---|---|---|---|
|理论适用性|非线性物理预计算，保留离散路线和连续调度|能直接描述全部决策|适合复杂多属性路径搜索|
|实际数据匹配|16节点仅240有向航段；箱多、短路线丰富，可缓存|80箱、14独立电池导致对称性和耦合多|80箱可用，异构与共享电池需定制|
|满足题目约束|主问题硬约束，候选不足须扩列|表达完整时可严格保证|必须修复/拒绝非法解，不能只用罚分|
|可解释性|每列是真实任务，资源甘特图可追溯|公式严谨，但求解黑盒较重|移动/插入的收益易说，最优性难证|
|计算复杂度|路线列总空间指数；限制候选池后MILP仍最坏指数|MINLP最坏指数且非凸|O(迭代×邻域评估×排程代价)|
|实现风险|候选池不全、析取M过松、过早删路线|载荷非线性、子回路、时序和求解器支持风险高|解码顺序偏置、后续硬期限被破坏|
|参数敏感|候选池、ε容忍、求解时间上限|线性化/数值容差/M边界敏感|移除比例、算子权重、接受温度、种子敏感|
|验证难度|精确验列+独立时间台账；小实例可证最优|完整模型核查难，但小规模可作参照|能验证可行，需多次运行与下界对照|
|比赛时间成本|中；需先有事件计算器，之后便于Q3扩展|高且不确定|中；快出解但调参数需时间|
|论文表达|模型与实现一致，界限/限制可清楚报告|容易出现“写了完整模型却未真正解”|需报告稳定性与可行性证据增强说服力|

选择A，C作为候选生成及备用整体求解器。这是同一个调度模型的分解求解，不把MILP、ALNS各自计为独立创新。**受限候选池的最优值不是原问题的全局最优值，其LP松弛也不是原问题有效下界**；只作为受限池内部证据。原问题下界需另行使用覆盖更大可行域的物理/资源松弛。B不作比赛主线。

多点路线长度不能凭经验永久截到2–3站：先生成短路线是搜索策略，若可行性或性能不足需增加更长路线，最终明确搜索覆盖范围。Q1最优组批不能当Q2不可修改的任务，因为3个小服务区可适合C型合并，而硬截止也可能要求拆分Q1批次。

## 4. 问题三：连续通信联合调度

- **A：候选悬停点分层细化 + 连续覆盖区间 + 联合路线/中继任务MILP与邻域迭代（主选）**。先按“直连不足的运输阶段”生成有针对性的悬停位置，再增加全域粗网格作覆盖补充；测试多个离地高度（不超过300m），筛掉回传或能源不可行点。以可证明覆盖的时间区间接入调度，允许运输与中继相互重排。
- **B：连续三维位置—轨迹—时序的一体化MINLP（理论参照）**。直接优化悬停经纬度、海拔与所有任务时刻。DEM遮挡是非光滑开关，非凸性远超过普通选址问题，仅适合在A选中点附近局部改进或少量点测试。
- **C：固定少量高地/300m中继点 + 区间覆盖调度 + 必要运输修复（备用）**。先固定易解释的点位再调服务时段。可较快产生基准，但若候选点不能覆盖下降/交接阶段必须扩大候选，不允许只补巡航。

|比较维度|A：离散候选+细化联合|B：连续一体化|C：固定点位分层|
|---|---|---|---|
|理论适用性|离散化连续选址，保留时间及资源耦合|理论可完整描述，DEM不光滑|合理可行启发式，不保证位置最优|
|实际数据匹配|2架中继、6能源，近195万像元不适合全部入MILP|超大栅格射线计算频繁|少数点覆盖小范围任务可能有效，但尚未验证|
|满足题目约束|须以整段覆盖证据筛选，不只节点采样|理论可约束“所有t”，直接实现困难|仍须连续覆盖/返航/充电验证|
|可解释性|候选点、覆盖区间和任务表清晰|局部解对初始点解释弱|位置固定，最容易说明|
|计算复杂度|约O(候选×轨迹验证点×视线像元)+组合调度|非凸混整+无限时间约束，代价最高|通信预计算较少，调度仍组合困难|
|实现风险|漏掉狭窄可用区、候选不足、区间验证错误|遮挡梯度无意义、求解不收敛|固定点先验使问题假性不可行|
|参数敏感|空间/高度分辨率、链路裕量、验证容差|初始位置、非凸求解参数极敏感|点位与高度选择敏感|
|验证难度|原始DEM独立复算与区间证书；中至高|最困难，不能凭优化器状态证明连续通信|与A相同验证标准，模型更简单|
|比赛时间成本|中至高，但可先粗后细逐步交付|很高，不确定性大|低至中，适合备用|
|论文表达|运输通信联动与代价比较有说服力|易产生公式完整、结果不可复现问题|可解释但需与至少一个替代布局比较|

主选A，备用C。不能把遮挡当作禁止连接，也不能把最大通信距离画成二维圆后视为充分条件。本题三类双向总损耗门限不同：直连122 dB、接入116 dB、回传126 dB；接入可能成为主要瓶颈，实际瓶颈需经地形验证后判断。无证据支持“只要升到300m就一定可通信”。

## 5. 问题四：冻结调度后的分区配置

- **A：运输同架次超边收缩 + 无标号分区枚举/分支定界 + 区间峰值计数（主选）**。同一运输架次所含服务区并查集合并；对至多15个不可拆分量分2/3组。每个分区独立复制跨组所需的冻结中继服务，按设备及能源的再可用区间计算最少数量。
- **B：任务组指派MILP + 事件时刻资源容量约束（备用）**。分量到组的0–1变量；中继架次是否被该组需求激活的OR变量；对每类资源在事件切片上的活跃数加容量上界，直接优化规模/缺口/均衡。
- **C：分量图上的均衡切分 + 交换局部搜索（快速基准）**。以运输机占用时间为主工作量、中继复制代价为跨组惩罚。不能直接对15个坐标K-means，因为可能切开同架次服务区。

|比较维度|A：收缩枚举|B：分区MILP|C：图切分+局部搜索|
|---|---|---|---|
|理论适用性|有限分区+固定区间资源计数，可得精确比较|线性逻辑和事件容量契合|可构造均衡分组，局部最优|
|实际数据匹配|最多15个分量：2组16383种，3组2375101种无标号划分，收缩后更少|组数仅2/3，规模适中|可快速初始分区|
|满足题目约束|收缩天然保证同架次同组，其余逐方案验证|可显式冻结日程并约束非空|需每步守住分量完整性|
|可解释性|枚举集合与峰值时刻易审计|变量和峰值约束直接|切分权重需解释|
|计算复杂度|S(m,K)×资源事件扫描；可缓存子集成本|最坏指数，事件矩阵较稀疏|近似多项式迭代|
|实现风险|中继跨组复制或能源充电区间漏算|OR变量、对称性、均衡线性化|忽视真实时间冲突，以地理距离代替资源需求|
|参数敏感|对固定Q3方案和冗余定义敏感，无种子|对目标ε、资源尺度敏感|对初始划分与惩罚权重敏感|
|验证难度|小m完全穷举；区间着色可给实体编号证明|可与A核对|可行容易验，最优差距难验|
|比赛时间成本|低至中；2组简单，3组需缓存/剪枝|中，已有MILP组件时适合|低|
|论文表达|资源缺口原因与峰值证据最清楚|多目标边界清晰|只给分区图而无台账则证据不足|

主选A。若3组全枚举成本超预算，B作精确备用；C只作为初始解。若运输收缩后分量数m<K，则在冻结前提下该K无可行划分，这是必须报告的结构结论，不能偷偷拆运输架次或返回Q3重新求解后仍称同一基础方案。

## 6. 理论依据及搜索边界

仅进行了理论方法定向检索，未搜索本届赛题标题、现成答案或参赛队方案。以下来源只支持方法适用性，不证明本题算法最优或给定库存必然足够：

1. Røpke与Pisinger（2006）的带时间窗取送货研究采用ALNS，为复杂路径邻域设计提供方法依据；本题独立电池和DEM能耗须自行扩展。[DTU作者机构记录，Transportation Science](https://orbit.dtu.dk/en/publications/an-adaptive-large-neighborhood-search-heuristic-for-the-pickup-an/)。
2. Pisinger与Røpke（2007）讨论统一求解多类车辆路径问题的ALNS框架，说明“移除—修复”可适配多约束，但不能据此推断本题性能。[DTU作者机构记录，Computers & Operations Research](https://orbit.dtu.dk/en/publications/a-general-heuristic-for-vehicle-routing-problems/)。
3. 自由空间传播的理论出处为ITU-R P.525；本题计算仍严格使用附件给定32.45、MHz、km和10 dB遮挡增量。[ITU官方P.525-5](https://www.itu.int/rec/R-REC-P.525-5-202411-I/en)。

上述为运筹学期刊研究与国际技术建议书，未核验特定年度JCR分区，不虚标Q1/Q2等级。无需为增加引用数量继续搜索。

## 7. 总体决策与触发条件

|问|主模型|何时启用备用|不得牺牲的条件|
|---|---|---|---|
|Q1|可行批次集合划分|列求解工具不可用或DP更直接时用数量状态DP|逐箱唯一、双容量、返航能量|
|Q2|路线列+连续资源MILP，邻域扩列|主问题求解过慢时用ALNS+资源解码器保留已验证可行解|硬时限、实体与独立电池台账|
|Q3|覆盖区间+候选点细化的联合调度|候选/求解过慢时先用固定点位，但需重新验证与修复运输|爬升/巡航/下降/交接全程通信及两段同刻可用|
|Q4|超边收缩+分区枚举+峰值资源核算|3组枚举慢时用事件MILP|冻结Q3、不得跨组调配|

最终模型是待确认的实施路线。可行性、运行时间与目标改善幅度均须在下一阶段实际求解后才能报告。
