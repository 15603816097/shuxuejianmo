# 接管审查与实施合同

权威路线为用户已确认的`03_最终模型方案.md`。本报告继承该文件全部数学定义，并给出编码所需的最小澄清；不重新选模型。01/02为数据与候选比较依据。原始20个文件哈希见01，禁止覆盖。本轮范围为模型确认、最小链路和可信初步结果，不是最终调优或论文交付。

## 接管十项结论

|检查|结论与处置|
|---|---|
|四问覆盖|完整：Q1能力/分箱/余量；Q2多点时限与双资源；Q3连续通信与中继；Q4冻结日程分区。初步结果仍必须区分基线与优化解|
|输入输出变量参数|03已定义；设备/能源ID可按库存规则生成，原始飞机ID保留；Q4不把任务架次当实体数|
|公式量纲|等效航程m；电量×距离/航程为kWh；mgh/(效率×3.6e6)为kWh；kW×s/3600为kWh；SOC无量纲|
|数据可得性|真实附件提供全部确定性参数；能耗分量、建链耗能等属于明确的闭合假设，无拟合系数|
|不可辨识与循环|不估计气象/通信噪声/未知功率；Q3从Q2热启动但允许反馈重排；Q4只能依赖冻结Q3，无逆向循环|
|依赖关系|Q1组批不锁死Q2，Q2不锁死Q3，Q4锁死Q3。Q4失败不得悄悄重排Q3|
|复杂度|Q1等价计数DP精确解集合划分；Q2受限路线列+资源事件解码作为首个可行基线，局部路线扩列与连续时序修复逐步加入；不宣称穷尽路线|
|验证|四问独立重算、约束断言、反例测试、小实例对照；通信用充分条件认证，抽样不能当连续证明|
|可编码性|03连续通信的扫掠视线认证需具体化；下述保守走廊界给可实现闭合。全局非凸选址不在首轮范围|
|此前关键问题|能耗闭合保留为假设；节点海拔以表为准；中继复制与严格合并两种Q4口径均报告。没有理由更换主模型|

## 必要澄清及首轮计算范围

1. **节点与航段**：WGS84经纬度采用O01中心的等距圆柱局部坐标，地球平均半径6371008.8m、参考纬度O01。直线穿越所有像元边界，含边界接触的像元保守并入；航段地形最高值精确于该栅格解释。运输端点海拔差绝不改Excel。中继巡航H取地形+50与两端高度最大值。
2. **Q1同属性压缩**：同区同类货物在Q1质量体积一致且不含时限，枚举各类数量的可行模式并作数量状态DP，与逐箱集合划分等价；按原箱号恢复。字典序(N,E,A)逐层比较。该措施只减少对称性，不改变模型。首轮跑基准及少量安全余量点，密集Pareto扫描留后续。
3. **Q2初步求解器**：先用硬截止优先+同区双容量组批构造基线，再生成跨区合并/移箱/换型候选，以实体和电池最早可用时间解码，重新检查全部硬截止后接受。主模型仍为路线列与双资源调度；事件解码是03已允许的备用求解手段，其结果是可行上界，非MILP最优证据。可增加固定路线顺序下的连续时间LP修复；若无改善如实报告。5个固定种子只作有限搜索，停止以固定迭代次数为准，避免时间上限造成复现差异。
4. **Q3连续覆盖可编码充分条件**：对一段线性运输运动端点P₀、P₁与固定通信端点G，先用两端最大距离和10dB最坏遮挡判断。否则认证扫掠视线无遮挡。设中点水平向量v=P_mid−G，L=||v||，移动半径δ=||P₁xy−P₀xy||/2。对像元中心x、像元半对角r，以距离线段(G,P_mid)≤δ+r选取包含整个扫掠三角形的保守像元走廊。任意可能交点在线段参数λ的界为clip(((x−G)·v)/L² ±(δ+r)/L,0,1)。视线最低高度由z_G+λ(z_P−z_G)在λ上下界和z_P两个端点高度四角取最小。若所有候选像元低于该下界，则全时间段无地形遮挡；再用两端最大距离检查无遮挡门限。不能认证则二分时间段，最小段仍不能认证的按未覆盖处理，记录未知，不反推为真实失联。仅δ=0且两运动端点水平位置均等于G时才作为垂直链路，用完整垂直柱相交像元和端点最小高度检查；L≈0但δ>0时必须二分，或用整个扫掠水平包围盒与所有端点最小高度作保守检验，不能仅查中心柱；d≈0时以连续极限判可用，避免log(0)。此算法可能误拒可行覆盖，但不会将未证区间说成已证。
5. **Q3首轮策略**：先搜索少量高地/服务点附近候选及50–300m高度，评估Q2阶段的连续可用区间；把需要同一中继的运输任务形成服务窗口，安排2架中继和6组能源并允许运输时移/局部重排。若该候选池不能形成完整可行方案，输出明确未覆盖/资源冲突诊断，不把失败等同原问题不可行。优先扩大有证据的瓶颈点池，有限次数后报告范围限制。
6. **Q4两口径**：主口径复制跨组所需的冻结中继任务、组内同型资源重新编号；严格口径把共享中继任务涉区也合并，若不足K个分量报告不可行。原ID保持资源上界另列。禁止用未认证的Q3方案声称正式Q4可行结果；若Q3只得到局部切片，Q4可做切片算法测试但必须标注。

## 假设分级

关键假设3条：①附件机型航程与能量支持03的水平+势能闭合；②题给DEM常高像元与规则定义测试场景，无额外天气/链路容量；③Q4采用冻结任务复制核算口径，同时保留严格解释。前两条变化需重算Q1–Q4，第三条变化需重算Q4。
可放宽假设5条：局部距离投影（对照球面距离）、恒定最大升降速率（速度扰动）、准备阶段占用满电能源（替代占用口径）、整站交接完成计逐箱交付（逐箱顺序扩展）、建链30s按1.10kW耗能（改1.05kW对照）。未执行敏感性不得写成结论。

## 模型合同摘要

|问|任务/结论类型|模型与数学定义|实现与参数|验证/回退|
|---|---|---|---|---|
|q1|45项能力与80箱单点组批|03第1/2节：单调能耗边界、每箱恰一次集合划分、min字典序(N,E,A)|二分质量容差1e-6kg；同属性DP；三机型容量和余量来自xlsx|逐箱覆盖、容量/SOC、余量单调、原始枚举小例交叉|
|q2|完整运输表与逐箱时刻|03第3节：路线列、同型实体/能源析取，医疗+首批硬截止；min(D,T,E,N)|精确路线事件函数、基线+多种子邻域扩列+资源解码；保留每次目标|重算物理/逐资源/31硬截止；无可行解则扩候选或报告失败|
|q3|联合任务与全程通信|03第4节+本报告充分条件：直连或一跳双链路，2中继/6能源，300m上限；min(D,T联合,E总,N总)|候选点细化、服务窗构造与运输修复、原始DEM连续认证|全阶段证书、建链/返航/充电资源；不能证明即拒绝或记录未知|
|q4|K=2/3分区及资源缺口|03第5节：超边连通分量、冻结区间峰值、归一缺口/配置/均衡|子集峰值缓存、无标签划分枚举；严格口径与原ID上界|15区唯一/同架次同组/任务字段不改；区间着色构造验证|

数据字段、全部公式、输入输出契约以03完整正文为组成部分；术语表见本目录另一固定产物。初步图按raw/process/result覆盖q1–q4，共12张逻辑图及总体流程图。所有优化留有基线、种子、参数、日志；正式结果不得手填。

## 理论与规则依据

现有02中的DTU作者机构文献与ITU官方来源继续适用，不重新搜赛题答案。双引擎理论检索实际命令与输出在logs/theory_search.json；任何引擎不可用只标“检索未完整”，不假造交叉验证。已有可追溯原始来源足以支持当前模型合同，无新增经验参数。

- Røpke/Pisinger, 2006, Transportation Science，ALNS时间窗方法：[DTU](https://orbit.dtu.dk/en/publications/an-adaptive-large-neighborhood-search-heuristic-for-the-pickup-an/)。
- [ITU P.525-5](https://www.itu.int/rec/R-REC-P.525-5-202411-I/en)：自由空间传播理论出处，数值规则以题面为准。
- [2026官方邀请函](https://cpipc.acge.org.cn/cw/contestNews/detail/4/2c9080189dcfa24e019dddacc24a1314?page=0)：竞赛至9月27日12:00，PDF的MD5提交截至同一时刻；PDF上传9月27日14:00至9月28日24:00。按北京时间安排，不能把上传截止当建模截止。核验时间2026-09-24 22:00 CST，距MD5约62小时；建议内容冻结9月26日18:00、最迟安全导出9月27日08:00。当前不做论文，故只维护可复现代码结果Checkpoint，不能称完整可提交作品。文件大小/支撑材料命名及最新补充公告待平台核验，不阻断数值计算。

## 作者自检与门禁

接管十项均有证据映射；无统计不可辨识问题；Q3覆盖认证已给具体充分条件，Q4歧义显式分场景。能耗闭合仍是模型条件，不虚构官方确认。完成M1独立验收后才进入最小代码；P1通过才全量；P2针对本轮初步结果验收，不要求完成密集敏感性、最终性能或论文。

M1首轮纠正：移动水平段穿过固定端点时中点距离可为0但并非垂直链路，退化分支已按独立质检反例修正。双引擎检索实况：OpenAlex HTTP429，AnySearch返回候选，cross_validated为空；已有DTU/ITU原始来源保留，新候选未用作参数或性能证据。
