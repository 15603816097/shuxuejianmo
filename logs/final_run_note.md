# 本轮最终运行记录

命令：`PYTHONPATH=.deps python src/minimal_pipeline.py --seed 20260924`

退出码：0。当前源码 SHA-256 已写入 `results/repro_manifest.json`。

关键结果：Q3 直连 0、经一跳中继 18、最终不可行 0；阈值 -3 dB 时直连 0、中继 11、不可行 7。Q4 原始 K=2/K=3 中继峰值均为 3（容量 2），调整后中继峰值均为 2，资源违规数为 0；调整造成部分 Q2 时限进一步违约（调整诊断 11 架次标记为 false）。

独立 P1 复核此前为 FAIL；本轮代码增加了 DEM 边界保守取值、网关高度、全程候选接入扫描和能源/服务资源表，但当前中继最小链路仍采用 33 个自适应检查点，不能据此宣称最终连续认证已经完成。该限制在交付汇报中保留。
