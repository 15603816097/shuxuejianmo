# 03 最终模型方案（待确认实施）

本文件给出四问拟采用的数学模型和求解路线，**不是已求得的比赛方案**。本轮只做全量数据审查和模型设计；尚无45项安全载荷结果、可行调度、优化目标值、连续覆盖证书或实际任务分区。数据依据与完整审查见01，候选比较及理论来源见02。

## 1. 共用物理层、符号与计算契约

### 1.1 对象与单位

节点集合V={0,1,…,15}，0对应O01；服务区编号严格保留S001–S015。箱集合B有80个，每箱b的目的地i(b)、质量m_b、体积v_b、优先系数w_b、期望时刻d_b、首批标签f_b与首批截止h_b均由逐箱表给定。机型g∈{A,B,C}；中继类型R。

长度内部用m，时间用s，质量kg，体积m³，能量kWh，功率kW；SOC内部为[0,1]，输出才乘100%；链路频率MHz、三维距离km、增益dBi、功率/灵敏度dBm、损耗dB。重力加速度g₀=9.81 m/s²为计算常数，不与机型下标混淆。

坐标采用WGS84，在O01附近建立米制局部平面供水平直线计算，保留逆变换定位到DEM；原始经纬度用于最终输出。实施时对最大节点间距离比较局部平面与椭球测地距离，记录误差。禁止直接将“经纬度欧氏差”当米。经度对应列、纬度对应行且行号向南增长；GeoTIFF的PixelIsPoint与MAT的外边界transform须统一到像元中心。

### 1.2 航段与阶段轨迹

令z_i为Excel给定地面海拔，作业海拔a_0=z_0，a_i=z_i+30（i>0）。运输节点对i≠j共240条有向航段。沿水平直线使用栅格穿越算法枚举**所有相交DEM像元**，不是只采端点或每隔100m取样；采用像元内常高的地形解释并对边界相交像元取保守最大值。

$$H_{ij}=\max_{c\in\mathcal C_{ij}}z_c+50,\quad
h^+_{ij}=H_{ij}-a_i,\quad h^-_{ij}=H_{ij}-a_j.$$

运输航段应检查两高度非负，异常时记录并按已声明的端点净空解释处理，不将负时间继续计算。中继到悬停点的作业海拔可超过地形+50，统一扩展为

$$H^R_{0r}=\max\{\max_{c\in\mathcal C_{0r}}z_c+50,a_0,z_r\}.$$

该扩展使中继先爬升—水平巡航—下降到指定悬停海拔；若需要采用不同的官方澄清口径，应整体替换，不能两问使用不同几何规则。

$$t_{gij}=h^+_{ij}/v_g^\uparrow+d_{ij}/v_g^c+h^-_{ij}/v_g^\downarrow.$$

采用附件最大升降速度作为计划恒定升降速度，作为可复现假设；题目没有给加速段，不额外引入加速度参数。按三阶段建立分段线性三维轨迹X_p(t)，投送时停留在a_i；每次投送后重新爬升，不能多站一直保持高空而省略升降。

### 1.3 运输能耗：题给部分与闭合假设分开

题给等效航程：

$$L_g(q)=L_g^0-(L_g^0-L_g^F)(q/Q_g)^{3/2},\qquad0\le q\le Q_g.$$

PDF附录2只给出总能耗由水平与爬升两项组成，未展开两分量的数值表达式。拟采用以下**物理闭合假设**：等效航程对应消耗一组可用能量；爬升电能等于增加的重力势能除以效率。

$$E_{gij}(q)=\underbrace{E_g^{use}\frac{d_{ij}}{L_g(q)}}_{E^{hor}}
+\underbrace{\frac{(M_g+q)g_0h^+_{ij}}{3.6\times10^6\eta_g}}_{E^{up}}.$$

M_g使用“含电池空载总质量”，不再另加电池；η_g=0.72。下降不增加额外能耗，不产生回收电能。此假设须在后续论文明确写出，并检查是否有官方澄清；本轮不把推导公式伪称为附件原文。

每架次的逐段剩余货物质量q_{pij}按实际交付递减，返航q=0：

$$E_p^T=\sum_{(i,j)\in p}E_{gij}(q_{pij}),\quad E_p^T\le(1-\rho_g)E_g^{use},\quad SOC_p=1-E_p^T/E_g^{use}.$$

体积也随交付下降，本题无途中取货，因此起飞容量约束足以保证以后容量不超限，但仍逐段记录供核对。运输交接计时间，基准不加未给出的运输悬停功率。全程能耗余量在返航后检查，不能只检查最远点剩余20%。

### 1.4 作业时间、交付时刻与能源充电

运输准备D_p^{prep}=300+30n_p。服务区i的交接时间s_{pi}=s_g^0+s_g^1n_{pi}，A/B为150+30n，C为180+36n。架次开始定义为准备开始，起飞=开始+准备；交付完成为到达该服务区后交接结束，默认该站各箱同时记为该时刻。架次返回时刻R_p包括所有飞行与交接；累计作业A=Σ(R_p−S_p)，不含待命/充电。Q2的完工T_T=max_p R_p；Q3的T_J=max(max_pR_p,max_rR_r)。不把最后充电结束或中继周转结束当任务完工。

能源资源充满才能再次参与准备，初始满电。任务后剩余SOC=s时：

$$C_g(s)=T_g^{full}\begin{cases}
0.65(0.90-s)/0.90+0.35,&0\le s<0.90,\\
0.35(1-s)/0.10,&0.90\le s\le1.
\end{cases}$$

运输实体占用[S_p,R_p)，能源占用[S_p,R_p+C_g(SOC_p))。不同能源并行充电、同一能源不与自身飞行重叠。半开区间允许一任务结束时刻接下一任务的准备。若真实电池只需起飞时装机，可作较宽松口径对照；基准为准备开始占用，所得需求属于此保守资源口径。

中继实体占用[S_r,R_r+300)，能源占用[S_r,R_r+C_R(SOC_r))；返航后的充电可与实体300s周转并行。180s准备与300s周转不互相替代。

### 1.5 优先级与硬时限

医疗箱集合M、首批集合F。硬约束：

$$C_b\le d_b\ (b\in M),\qquad C_b\le h_b\ (b\in F).$$

两类交集同时满足；本数据硬约束箱并集31箱。非医疗箱仍可有首批硬截止。非医疗期望时刻用于软目标：

$$D=\frac{\sum_{b\notin M}w_b\max(0,C_b-d_b)}{\sum_{b\notin M}w_b}.$$

D单位为s；权重只来自附件，不加人口或主观类别权重以免重复加权。医疗早送效果可另报，但不通过软罚分放松硬期限。若全部非医疗准时则D=0，后续按完工/能耗/架次区分解。

## 2. 问题一最终方案

### 2.1 最终采用模型与原因

**单调能耗边界 + 可行批次集合划分0–1规划。**每个服务区最多15箱，完整枚举子集在本题规模内合理；将非线性能耗预计算为批次常数，可避免直接求MINLP。逐箱表示便于导出模板和与Q2交接。

- **输入/所需数据**：节点及海拔、原始DEM、3种运输机参数、逐箱目的地/质量/体积；不使用实体及共享电池数约束。
- **输出**：45项能力边界、各区逐箱组批/机型、每架次质量/体积/往返飞行与总作业时间/E/SOC，以及余量扫描的非支配方案。
- **变量**：连续能力q；批次p是否选用x_p∈{0,1}。
- **参数**：Q_g、V_g、M_g、L_g^0、L_g^F、E_g、ρ_g、速度/效率、装载交接常数、DEM航段数据。无数据拟合参数。

### 2.2 核心公式

先计算连续质量能力：

$$\bar q_{gi}(\rho)=\max\{q\in[0,Q_g]:E_{g0i}(q)+E_{gi0}(0)\le(1-\rho)E_g^{use}\}.$$

E(0)已超过阈值则该组合不可行；E(Q_g)不超过阈值则能力为额定载重；其余在[0,Q_g]二分，建议质量误差≤0.001 kg，同时按保守侧取值。该误差仅是拟定算法容差，非仪器精度。

枚举非空p⊆B_i并选择g；保留Σm_b≤q̄_gi且Σv_b≤V_g的批次，随后直接重算精确能量以防二分容差导致越界。p含机型标签，同一箱集合可以有多种机型列。覆盖约束：

$$\sum_{p:b\in p}x_p=1\quad\forall b,\qquad
N=\sum_px_p,\ E=\sum_pE_px_p,\ A=\sum_pA_px_p.$$

基准按字典序min(N,E,A)，即先解最小架次N*，固定N*再最小E，最后在前两目标数值容差内最小A。该优先顺序是本路线的决策偏好，不是题目唯一要求。另允许N≤N*+1、N*+2，绘制/列表E和A的交换关系；每个固定架次数再以A上界扫能耗，避免声称一个字典序点代表全部Pareto前沿。

### 2.3 求解算法

1. 预计算15条去程及返程几何；二分45项安全能力。
2. 每区生成所有非空箱子子集，按机型过滤质量、体积、能源；只对完全相同箱集合删除严格被支配机型列。
3. 按区解集合划分，汇总基准；在全局架次数ε预算下分配额外架次，不简单要求每区各增加同样架次。
4. 安全余量扫描ρ∈{0.10,0.15,0.20,0.25,0.30}，明确非基准情景不代表原题允许降低安全性。对结构发生变化处细扫。
5. 对每个候选批次还可计算临界余量ρ_p^crit=1−E_p/E_g^{use}，得到可行批次随余量退出的明确阈值。

### 2.4 验证、备用与风险

- **验证方法**：全部80箱恰一次；每批单区；双容量与能源独立复核；E随q非减、能力随ρ非增；ρ提高时完整可行域嵌套，最小架次不能下降。随机挑小区穷举分组，或与数量状态DP逐区交叉验证；检查物理单位和q=0/Q边界。
- **备用模型**：按四类箱数量状态DP，输出时从同属性原始箱号恢复逐箱映射。Q1不含时限，可等价压缩；该压缩不可不加区分直接挪到Q2。
- **潜在风险**：忽略体积；把空载不能返航标成q=0；把Q1组批锁入Q2；把未展开的能耗假设当官方公式；用累计时间冒充并行完工时间。

## 3. 问题二最终方案

### 3.1 最终采用模型与原因

**可行路线列集合划分 + 连续时间实体/能源调度MILP，由大邻域生成新列。**能耗随剩余载荷非线性，但每个确定箱集/访问顺序/机型的路线可精确预计算；主问题不必对载荷函数做粗糙线性化。80箱规模适合限制候选池并迭代改善。

- **输入/所需数据**：共用物理层、80箱全部属性、8个实体ID、A/B/C电池数6/4/4、充电参数；Q1列和方案只作初始候选与基准。
- **输出**：所有路线、机型/实体/电池、准备开始/起飞/交接完成/返航、逐箱交付时间和资源台账、D/T_T/E/N及硬约束检查结果。
- **变量**：选路线x_p；开始S_p≥0；实体指派y_pu；电池指派z_pk；同资源路线先后δ_pp'；每箱完成C_b、迟到ℓ_b；完工T_T。
- **参数**：列的箱集合B_p、机型g(p)、持续时间τ_p、能耗E_p、箱相对交付时刻a_bp、充电耗时c_p；资源类型和库存；d_b/h_b/w_b。

### 3.2 核心公式：路线列、目标与约束

每列p为(箱ID集合，访问序列0→i₁→…→i_l→0，机型)。同一服务区在一列中原则上只访问一次，在该站交付该列所带全部目的地箱；其他架次可再访问同一区。若要宣称该简化不损失一般性，应依据纯配送、无提前释放时间及无途中充电结构证明；否则报告为搜索范围限制，并在困难实例允许重复站列。

所有列先满足起飞重量/体积及完整往返能量；记录各箱相对交付完成时刻a_bp和τ_p。主问题：

$$\sum_{p:b\in B_p}x_p=1;\qquad
\sum_{u:g(u)=g(p)}y_{pu}=x_p,\quad
\sum_{k:g(k)=g(p)}z_{pk}=x_p.$$

若选p，C_b=S_p+a_bp（b∈B_p），R_p=S_p+τ_p。通过指示约束激活等式，未选路线不能影响最大完工或箱时限。若p、p'占同一实体，必须

$$S_{p'}\ge R_p\quad\text{或}\quad S_p\ge R_{p'}.$$

若占同一电池，必须

$$S_{p'}\ge R_p+c_p\quad\text{或}\quad S_p\ge R_{p'}+c_{p'}.$$

以上析取用0–1先后变量/指示约束实现；如果使用Big-M，先以经过可行性检查的上界日程确定有限时间域再给M，不能任意取10⁹。找不到初始可行上界时逐步扩大显式时间域，并保持硬期限不变；软期望18000 s不是所有任务必须结束的总时域上限。

硬期限见1.5；ℓ_b≥C_b−d_b、ℓ_b≥0；T_T≥R_p（仅选列）；E_T=ΣE_px_p，N_T=Σx_p。

基准字典序min(D,T_T,E_T,N_T)，另按D≤D*+ε_D、T_T≤T*+ε_T求更低能耗与架次数，其中拟议ε_D={0,60,180}s、ε_T={0,300,600}s。仅作可解释的方案比较，所有医疗与首批硬时限永不放松；若D*=0，应单列完全准时方案，不能仅为省能耗默认接受迟到。

### 3.3 求解算法

1. 建立独立“给定箱集+序列+机型→阶段事件/E/SOC”的精确计算器。
2. 用最早硬截止优先、带后悔值的插入生成可行初始调度，允许不同机型和同型电池换装；同时保留Q1单点列。初始贪心失败不等于问题无解。
3. 枚举/生成短多点列，保留硬截止可满足的列；求受限主问题。
4. 以移箱、拆并批、换型、两站次序交换、删除迟到相关路线再插入等邻域增加列；每次都重算新列的逐段载荷和能耗。
5. 周期性重解实体/电池时序；若无可行解先扩大列池和路线站数，再诊断资源/截止冲突。搜索按时间预算停止，保存历次最佳已验证可行解、候选池范围、种子与求解gap。
6. 在ε网格上保留非支配方案，输出台账供独立验证。

这属于受限路线池与邻域搜索，不冒称实现了带严格定价的完整列生成/分支定价。只有覆盖完整路线域并有有效下界时，才可讨论原问题全局最优。

### 3.4 验证、备用与风险

- **验证方法**：80箱交付唯一、目的地一致；31个硬截止箱逐个核验；逐段载荷/E重算；每个实体/电池按时间排序核验不重叠，SOC≥0.2、再用满电；比较离散事件台账与模型输出。构造1–3服务区缩小实例，完整枚举与主算法交叉；与单点优先基线和简单插入基线在相同口径对比。
- **独立下界**：容量下界N≥max(ceil(758/80),ceil(2.011/0.25))=10（弱下界，不保证存在10架次方案）；每箱最快可行物理交付时刻给时限必要条件；以放宽资源和路线限制的模型给有效完工/成本下界。受限路线池LP值不可当原问题下界。
- **备用模型**：ALNS+确定性资源事件解码器；先确保硬约束可行，再改进同一目标，不以有限罚分容忍违规。拟5个固定种子对照，必要时扩大，报告最好/中位/最坏而非只给最好一次。
- **潜在风险**：候选池缺好路线；充电被误计成无人机停机；电池数量被多加；首次准备被漏；把到达当交付；小数时间取整导致硬期限越界。输出保留足够精度，验证使用未四舍五入值。

## 4. 问题三最终方案

### 4.1 最终采用模型与原因

**三维双向链路机理 + 自适应候选悬停点及覆盖区间 + 联合运输/中继资源调度。**只把有意义的候选悬停点进入优化，避免近195万DEM像元全域组合爆炸；运输路线和时刻可反馈调整，不强行给Q2固定解补两架中继。

- **输入/所需数据**：Q2全部数据与候选解；原始DEM；网关O01地面+20m；通信参数14项；中继R的23.5kg、3.2kWh、速度、功率、300m离地上限、2架实体与6组能源。
- **输出**：重新优化的运输及逐箱表、中继悬停/飞行/服务/资源安排，运输各阶段通信区间与最差裕量，D/T_J/E_T+E_R/N_T/N_R。
- **变量**：Q2全部变量；候选点j及高度z_j选择，中继任务r的S_r/B_r/F_r/R_r（准备开始/建链完成/服务结束/返航），实体与能源指派、每通信区间的保障模式。
- **参数**：通信设备门限、各候选点往返几何/E飞行、固定准备180s、建链30s、周转300s、悬停1.05kW和通信0.05kW、ρ_R=0.2、充电1800s；空间网格/验证容差为算法参数，非真实测量精度。

### 4.2 通信核心公式及附件数值核算

任意主体a→b：

$$P_b^{th}=P_b^{sens}+M_b,\quad
L^{max}_{a\to b}=P_a^t+G_a^t+G_b^r-L_{sys}-P_b^{th},\quad
L^{max}_{a\leftrightarrow b}=\min(L^{max}_{a\to b},L^{max}_{b\to a}).$$

$$L_{ab}^{path}(t)=32.45+20\log_{10}(2400)+20\log_{10}(d_{ab}^{3D}(t)/1000)+10o_{ab}(t).$$

其中o=1仅表示视线受DEM遮挡；不是直接置链路不可用。可用条件L_path≤L_max。设备接收门限统一−98+8=−90dBm，得到：

|链路|双向门限dB|无遮挡距离上限km|遮挡距离上限km|
|---|---:|---:|---:|
|运输—G01直连|122|12.511|3.956|
|运输—中继接入端|116|6.270|1.983|
|中继回传端—G01|126|19.828|6.270|

这些是公式核算，不是实际覆盖半径；实际可用必须结合当时三维距离和地形。接入/回传使用各自接口，不能把8dBi回传增益用于6dBi接入。

令A_pG(t)、A_pr(t)、A_rG(t)为可用性；运输任意时刻必须满足

$$A_{pG}(t)+\sum_r u_{pr}(t)\ge1,\quad
u_{pr}(t)\le A_{pr}(t),\ u_{pr}(t)\le A_{rG}(t),\ u_{pr}(t)\le \mathbf1\{B_r\le t\le F_r\}.$$

实际建模采用互斥直连变量d_p(t)与Σ_ru_pr(t)，使d_p+Σ_ru_pr=1，且d_p≤A_pG。直连可用时优先记直连，未用的冗余中继覆盖不重复计服务。任何时刻不得接两架中继，不得多跳；题给没有中继并发容量，所以允许一个已建链中继同时为多个运输机提供可用链路。

### 4.3 中继能耗与任务时序

对给定悬停点r，去/回飞行时间按阶段求得t_out/t_in，准备开始S_r，建链完成

$$B_r=S_r+180+t_{out}+30,\quad F_r\ge B_r,\quad R_r=F_r+t_{in}.$$

水平飞行能耗=1.15×(水平飞行秒数)/3600，爬升附加能耗按23.5kg势能/效率公式，下降附加为0。拟将建链阶段按悬停+通信功率计入：

$$E_r^R=E_{out}^{fly}+E_{in}^{fly}+(1.05+0.05)\frac{30+F_r-B_r}{3600}\le0.8\times3.2.$$

因此该点单次最大可服务秒数

$$F_r-B_r\le\frac{3600(2.56-E_{out}^{fly}-E_{in}^{fly})}{1.10}-30.$$

右端负值则该点无可行中继任务。180s地面准备与返航后300s周转不计飞行功率。能源组件充电按实际E而不是每次默认从20%开始；中继位置必须在DEM内且0<z_r−z_DEM(r)≤300m，正下限只是避免地面点被称为悬停，不能声称官方规定了某个最小离地高度。

### 4.4 连续通信的可执行验证路线

**禁止只检查服务区、巡航端点或每秒抽样后声称“连续”。**默认地形为原始像元常高，对X_p(t)每个线性运动段作如下处理：

1. 固定/中继端点已知时，枚举移动视线扫过的像元；在时间区间I内，用区间界限检验每个可能相交像元的视线高度最低值是否高于地形。不能排除接触或遮挡时标记“未知/可能遮挡”。
2. 若整个I的最大三维距离不超过该链路遮挡状态距离上限，直接按最坏10dB证明全区间可用，无需证明无遮挡。
3. 否则须证明全I无遮挡，并以最大三维距离检查无遮挡门限。在线性端点轨迹和固定对端时，距离范数为凸函数，区间最大值可在两端取最大；遮挡仍需单独认证。
4. 不能认证则在地形状态变化/链路阈值附近二分时间区间。发现明确失联即反馈调度；到预设最小时间宽度仍未知时，**按不可覆盖处理并留日志**，不能以采样成功替代证明。
5. 对原DEM下每个运动/交接闭区间形成可用区间并集，核验联合覆盖无空隙；切换时点须至少有一个模式在该点有效。多个中继服务窗口拼接时还要验证建链已完成与同刻回传。

实作先用较密采样定位风险，再用上述保守区间/几何认证终审。拟比较1s、0.5s采样仅用于诊断，并将最小细分宽度设为可配置（例如0.01s）；小于该值的未决段仍按不通过。若区间几何实现时间不足，可用全区间“最坏遮挡距离”作保守认证备用，可能损失解质量，但不会将未经证明的无遮挡假装成立。

独立报告最小损耗裕量min_t(L_max−L_path)、最小地形视线裕量、未覆盖时长与未决区间数；数值容差不能把负裕量判为正。DEM分辨率之外的树木、天气、干扰等不在题给模型内，结论不外推为实际飞行安全认证。

### 4.5 联合求解算法与目标

**有限覆盖约束的具体落地。**地形和中继位置不随绝对时间变动，因此可对每条运输候选p预计算相对起飞/准备时间轴上的直连区间及各候选点的中继可用区间。将这些已认证区间的全部边界合并成原子区间h=[α_ph,β_ph]。对直连不满足的原子区间，只允许选择能覆盖整个区间的中继任务r；令u_phr为其0–1保障变量，则

$$\sum_{r\in\mathcal R_{ph}}u_{phr}=x_p,\quad u_{phr}\le x_r^R,\quad
u_{phr}=1\Rightarrow B_r\le S_p+\alpha_{ph},\ F_r\ge S_p+\beta_{ph}.$$

这里所有时间偏移均相对于同一定义的准备开始S_p，飞行前地面准备不纳入连续通信要求。空的兼容集合意味着当前路线候选在当前中继点池下不能选，须扩点或改路线。原子区间若长到需要换班，可进一步切成更短片段，使不同中继连续接力；划分精度及接力边界仍由连续校验认证，不把任意粗时间格当题给约束。相邻片段选择不同中继不引入额外切换时间，因题目未给此参数；另可用少量重叠作为稳健情景。直连片段固定为直连，不计中继占用需求。


1. 从Q2可行候选计算直连不足的完整阶段，而非只看巡航。
2. 以失联段端点、服务区邻近高地及水平中点生成候选，再加入DEM全域粗网格补充；初始离地高度可用50/100/200/300m，均是算法候选而非最终限制。先筛回传、往返能源，再筛运输覆盖；对被选点和未覆盖段周边细化水平/高度。
3. 生成“点位+覆盖需求时段”的中继任务候选，允许一次任务覆盖多个运输架次的时段，计中间空闲悬停能耗。服务不能在多个不连续时段凭空暂停耗能。
4. 主问题联合选择运输列和中继任务，安排开始时刻，满足覆盖同步、两类实体/能源析取与硬时限。中继时长影响能耗/SOC/充电时间：可用两段分段线性充电关系或对给定服务窗预计算，不能冻结成错误常数。
5. 对未知覆盖或资源冲突生成约束/替代列，反馈调整运输开始、组批、顺序和中继窗口；联合搜索收敛后做原DEM连续终审。
6. 字典序min(D,T_J,E_T+E_R,N_T+N_R)，并分别报告N_T、N_R；用同Q2的时间ε比较方案，不把任务架次数当实体数量。保留Q2基准和“固定Q2仅补中继”对照，量化联合重排价值。

### 4.6 验证、备用与风险

- **验证方法**：继承Q2所有校验；中继高度、往返净空、建链与服务先后、两类能源再用满电、2架/6组件占用；原DEM全阶段连续认证；对候选网格加密比较目标稳定性。只保证候选域内结果，不声称连续选址全局最优。
- **备用模型**：固定少量可认证悬停点，联合调整运输时序和中继班次；若只在最坏遮挡距离内可证明覆盖，采用保守覆盖并说明代价。
- **潜在风险**：连续性验证最难；低空交接遮挡、能源时长与任务时刻耦合、候选点漏好位置、初始中继赶不及早期爬升、末班中继返航使联合完工晚于运输完工。不得给未建链中继提前计覆盖。

## 5. 问题四最终方案

### 5.1 最终采用模型与原因

**运输任务超边收缩 + 非空分区枚举/分支定界 + 固定资源区间峰值（区间图着色）。**时间和路线在本问冻结，资源配置无需再次解路径问题；同型资源可相互替代时，固定占用区间的最大重叠数就是最少实体数量，可给组内编号作为构造证明。

- **输入/所需数据**：Q3经终审的全部任务及通信区间、机型、各箱映射、能耗/充电时长/周转；库存向量I=(4,2,2,6,4,4,2,6)。
- **输出**：K=2、3各组区域、冻结任务清单、按(A/B/C机、A/B/C电池、中继机、中继能源)排序的8维资源向量，复制中继映射、配置规模/冗余/缺口/均衡及峰值发生时刻。
- **变量**：不可拆分量c所属组a_ck∈{0,1}；各组资源n_kr∈整数；中继任务是否属于/复制到组；同型资源组内重新编号。
- **参数**：所有运输和中继任务时空安排固定；各任务占用区间及每一中继服务关联均固定；不存在新的航段、能耗或交付决策。

### 5.2 冻结与跨组中继解释

由每个运输架次涉及的服务区集合建超边，取连通分量C₁,…,C_m；跨多个运输架次的传递关联也必须合并。分组满足Σ_k a_ck=1和Σ_c a_ck≥1。如果m<K则不可行，直接报告，不能拆路线。

若冻结中继任务r为两个组的运输任务提供保障，原实体不能跨组共享。基准解释为：**每个涉及的组独立配置一个该中继任务副本**，保留相同悬停点、飞行海拔、准备/建链/服务/返航时刻及全窗口能耗；副本只承担本组运输通信，复制是独立执行资源核算，并不缩短/移动原服务窗口。这可能产生明显冗余与缺口，正是需分析的成本。

题面没有明确“跨组共享中继可否复制”的措辞，因此必须同时列出严格解释：如认为复制本身改变了中继任务安排，则把共享该中继任务的服务区也合并，重新判断K=2/3是否可分。这不是自动采用的额外硬约束；两种解释的结果须明确分开，必要时以官方澄清为准。亦不得把不同时间服务两组解释成可以把同一实体轮流借给两组。

对实体身份的冻结同样分开：主模型冻结任务安排但允许组内同型资源重新编号，以算“独立执行最少数量”；另按Q3原资源ID逐组去重并在跨组时复制ID，给出身份完全保持的配置上界。若官方要求身份也冻结，以上界口径作为正式配置，而非用重新着色少报资源。

### 5.3 核心公式

令J_kr是组k类别r的占用区间集合，运输实体[S_p,R_p)，电池[S_p,R_p+c_p)；中继实体[S_r,R_r+300)，能源[S_r,R_r+c_r)。则

$$n_{kr}=\max_t\sum_{j\in J_{kr}}\mathbf1\{t\in[s_j,e_j)\}.$$

按端点扫描即可求值，同一时刻先释放后占用。颜色复用按最早结束的空闲资源赋新任务，构造达到该峰值的资源清单。对共享中继复制，分别在各组计完整区间；不按飞行次数直接当资源数量，也不只看飞行并发忽略充电。

分区总配置N_r=Σ_kn_kr，未分区固定同一Q3任务最少配置n_r^global，资源额外需求与库存缺口分别为

$$R_r^{extra}=N_r-n_r^{global},\qquad
\Delta_r=\max(0,N_r-I_r),\qquad S_r^{stock}=\max(0,I_r-N_r).$$

n_global不一定等于Q3实际用了多少不同ID，先计算同口径最少需求再比较。冗余R_extra与库存余量S_stock不是同一概念；电池和无人机不同类型的件数不相互抵消。

主工作量采用运输实体作业占用W_k^T=Σ_p(R_p−S_p)，另报中继作业W_k^R及质量/箱数/人口，不因S001人口大就强制每组人口接近。

$$CV_W=\frac{\sqrt{K^{-1}\sum_k(W_k^T-\bar W)^2}}{\bar W},\qquad\bar W=K^{-1}\sum_kW_k^T.$$

配置以向量非支配比较为主；需要一个推荐方案时，拟采用字典序：先最小归一缺口Σ_rΔ_r/I_r，再最小归一配置Σ_rN_r/I_r，再最小CV_W。这是“按现有库存比例衡量稀缺性”的明确决策规则，不是经济成本或专家权重；同时保留完整8维结果，并对“中继优先/运输优先”的目标顺序交换作稳健性比较，避免标量掩盖关键单型短缺。

### 5.4 求解算法

1. Q3最终方案加版本号/哈希，冻结；抽出所有原任务ID与通信关联。
2. 并查集合并运输超边；报告m和每个不可拆分量。
3. 对每个分量子集预计算其运输区间、所需中继副本集合、8维峰值和工作量；最多2^15=32768个子集，可缓存。
4. 枚举无标号2组/3组非空划分，固定第一个分量在第1组并按最小编号排序消除组标签对称；用库存缺口下界、资源峰值下界剪枝。
5. m=15的最坏候选数S(15,2)=16383、S(15,3)=2375101；实际多点运输会收缩m。若时间超预算，换事件时刻MILP，不把提前终止当完整穷举最优。
6. 提取非支配分区与推荐分区，对每组区间着色构造可执行资源编号。做严格禁止中继复制/原ID保持的解释对照。

### 5.5 验证、备用与风险

- **验证方法**：15区恰归一次、每组非空、每个运输超边同组；比较Q3/Q4原任务字段确保箱批/路线/时刻/海拔不改；各组独立重新执行资源与通信校验；峰值时刻列出发生冲突的具体任务；配置数必须不小于任一时刻并发数。小分量实例手工枚举对照。
- **备用模型**：分量指派MILP，将所有固定区间端点切成事件时间片；每组每类活跃任务数≤n_kr，复制中继按“该组至少一条关联运输任务”的OR约束激活。
- **潜在风险**：把K-means坐标划分当合法任务划分；共享中继漏复制；只统计飞行峰值漏充电/周转；把库存缺口强制为0而掩盖分区代价；擅改Q3日程；没有足够可拆分量却编造3组方案。

## 6. 下一阶段实施顺序与验收门槛

|顺序|拟实施内容|通过条件|
|---|---|---|
|1|规范数据与共用事件/能耗计算器|输入哈希一致；箱/需求对账；DEM定位无半像元错位；物理极限和单位检查通过|
|2|Q1批次与能力|45项状态可追溯；80箱恰一次；完整约束校验；余量变化符合可行域嵌套|
|3|Q2实体/电池调度|至少一个完整可行方案；31硬截止箱全部满足；逐资源不重叠与SOC正确|
|4|Q3链路与联合调度|运输全阶段无中断且无未决区间；中继全部资源与返航可行；不能只用离散抽样验收|
|5|Q4冻结分区|K=2/3给合法配置或结构不可行证明；字段冻结核对通过；资源峰值/复制关系可追溯|

本轮未执行上述优化实现。审查脚本是数据读取和统计，不是求解器。建议后续Python完成数据、优化与验证，MATLAB可直接使用已核对的MAT；语言选择不改变本轮数学方案。等待用户确认路线后再进入完整编程，最终论文另行开始。
